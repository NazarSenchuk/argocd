namespace: argocd

extraArgs:
  - --interval
  - 1m

rbac:
  create: true

config:
  registries:
    - name: Docker Hub
      credentials: pullsecret:argocd/dockerhub-secret
      api_url: https://index.docker.io
      ping: yes
      default: true
