namespace: argocd

extraArgs:
  - --interval
  - 1m

rbac:
  create: true

config:
  registries:
    - name: Docker Hub
      credentials: secret:argocd/dockerhub-secret
      api_url: https://registry-1.docker.io
      ping: yes
      default: true
