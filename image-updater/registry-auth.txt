kubectl create  secret docker-registry dockerhub-secret \
  --docker-username name \
  --docker-password pat \
  --docker-server "https://index.docker.io"


#Set up the secret namespace and name into image updater credentials
#Updater must be in argocd namespace
